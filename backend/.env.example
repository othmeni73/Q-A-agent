# .env is for runtime/deployment-specific values and secrets.
# Non-secret app config lives in backend/config.yaml (committed).

NODE_ENV=development
PORT=3001

# Added in later steps:
# OPENROUTER_API_KEY=sk-...
# GOOGLE_GENERATIVE_AI_API_KEY=...
# HF_TOKEN=...